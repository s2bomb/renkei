{
  "name": "@renkei/engine",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test || true",
    "test:unit": "bun test --filter unit || true",
    "test:integration": "bun test --filter integration || true",
    "typecheck": "bun x tsc --noEmit",
    "lint": "bun x prettier --check \"src/**/*.ts\" --config config/prettier.json --ignore-path .prettierignore"
  }
}
