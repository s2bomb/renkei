# Validation Must Be Independent

Implementation self-attestation is a weak completion signal.

Completion claims are trustworthy only when validation independently checks code against plan and source requirements.

Without independent validation, unresolved requirement gaps can ship silently.
